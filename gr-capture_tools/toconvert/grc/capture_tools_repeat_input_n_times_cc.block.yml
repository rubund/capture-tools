# auto-generated by grc.converter

id: capture_tools_repeat_input_n_times_cc
label: repeat_input_cc
category: '[capture_tools]'

parameters:
-   id: n_times
    label: Times to repeat
    dtype: int
    default: '0'
-   id: max_samples
    label: Max samples to repeat
    dtype: int
    default: '10000000'
-   id: propagate_end_tag
    label: Propagate end tag through block
    dtype: bool
    default: 'False'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: add_repeat_tag
    label: Add repeat tag
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import capture_tools
    make: |-
        capture_tools.repeat_input_n_times_cc(${n_times}, ${max_samples})
        self.${id}.set_tag_propagate_end(${propagate_end_tag})
        self.${id}.set_tag_add_repeat(${add_repeat_tag})

file_format: 1
