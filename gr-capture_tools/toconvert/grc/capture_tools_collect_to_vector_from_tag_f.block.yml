# auto-generated by grc.converter

id: capture_tools_collect_to_vector_from_tag_f
label: collect_to_vector_from_tag_f
category: '[capture_tools]'

parameters:
-   id: len
    label: len
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float
    vlen: ${ len }

templates:
    imports: import capture_tools
    make: capture_tools.collect_to_vector_from_tag_f(${len})

file_format: 1
