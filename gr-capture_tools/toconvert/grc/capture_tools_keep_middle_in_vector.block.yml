# auto-generated by grc.converter

id: capture_tools_keep_middle_in_vector
label: keep_middle_in_vector
category: '[capture_tools]'

parameters:
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: keep
    label: Keep N
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

templates:
    imports: import capture_tools
    make: capture_tools.keep_middle_in_vector(${vlen}, ${keep})

file_format: 1
