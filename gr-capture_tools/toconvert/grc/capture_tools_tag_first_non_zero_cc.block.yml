# auto-generated by grc.converter

id: capture_tools_tag_first_non_zero_cc
label: tag_first_non_zero_cc
category: '[capture_tools]'

parameters:
-   id: tag_str
    label: tag
    dtype: string

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import capture_tools
    make: capture_tools.tag_first_non_zero_cc(${tag_str})

file_format: 1
