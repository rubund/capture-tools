# auto-generated by grc.converter

id: capture_tools_clock_recovery_mm_sync_ff
label: Clock Recovery MM Sync
category: '[capture_tools]'

parameters:
-   id: omega
    label: Omega
    dtype: real
    default: samp_per_sym*(1+0.0)
-   id: gain_omega
    label: Gain Omega
    dtype: real
    default: 0.25*0.175*0.175
-   id: mu
    label: Mu
    dtype: real
    default: '0.5'
-   id: gain_mu
    label: Gain Mu
    dtype: real
    default: '0.175'
-   id: omega_relative_limit
    label: Omega Relative Limit
    dtype: real
    default: '0.005'

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float
-   label: omega
    domain: stream
    dtype: float
    optional: true
-   label: mu
    domain: stream
    dtype: float
    optional: true
-   label: mu_end
    domain: stream
    dtype: float
    optional: true

templates:
    imports: import capture_tools
    make: capture_tools.clock_recovery_mm_sync_ff(${omega}, ${gain_omega}, ${mu},
        ${gain_mu}, ${omega_relative_limit})
    callbacks:
    - set_omega(${omega})
    - set_gain_omega(${gain_omega})
    - set_mu(${mu})
    - set_gain_mu(${gain_mu})

file_format: 1
