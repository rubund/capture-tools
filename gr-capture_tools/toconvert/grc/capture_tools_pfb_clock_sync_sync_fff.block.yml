# auto-generated by grc.converter

id: capture_tools_pfb_clock_sync_sync_xxx
label: Polyphase Clock Sync Keep Rate
category: '[capture_tools]'

parameters:
-   id: type
    label: Type
    dtype: enum
    options: [fff]
    option_labels: [Float->Float (Real Taps)]
    option_attributes:
        input: [float]
        output: [float]
        taps: [real_vector]
    hide: part
-   id: sps
    label: Samples/Symbol
    dtype: real
-   id: loop_bw
    label: Loop Bandwidth
    dtype: real
-   id: taps
    label: Taps
    dtype: real_vector
-   id: filter_size
    label: Filter Size
    dtype: int
    default: '32'
-   id: init_phase
    label: Initial Phase
    dtype: real
    default: '16'
-   id: max_dev
    label: Maximum Rate Deviation
    dtype: real
    default: '1.5'
-   id: osps
    label: Output SPS
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: ${ type.input }

outputs:
-   domain: stream
    dtype: ${ type.output }
-   label: err
    domain: stream
    dtype: float
    optional: true
-   label: rate
    domain: stream
    dtype: float
    optional: true
-   label: phase
    domain: stream
    dtype: float
    optional: true

templates:
    imports: import capture_tools
    make: capture_tools.pfb_clock_sync_sync_${type}(${sps}, ${loop_bw}, ${taps}, ${filter_size},
        ${init_phase}, ${max_dev})
    callbacks:
    - update_taps(${taps})
    - set_loop_bandwidth(${loop_bw})

file_format: 1
