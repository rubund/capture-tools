# auto-generated by grc.converter

id: capture_tools_file_sink_text
label: file_sink_text
category: '[capture_tools]'

parameters:
-   id: size
    label: Size
    dtype: int
-   id: nelem
    label: Vector length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: float
    vlen: ${ nelem }

templates:
    imports: import capture_tools
    make: capture_tools.file_sink_text(${size}, ${nelem}, "")

file_format: 1
