# auto-generated by grc.converter

id: capture_tools_add_rampup_cc
label: add_rampup_cc
category: '[capture_tools]'

parameters:
-   id: alpha
    label: alpha
    dtype: float
-   id: constant_start
    label: Constant start
    dtype: int
    default: '-1'
-   id: constant_stop
    label: Constant stop
    dtype: int
    default: '-1'
-   id: constant_length
    label: Constant length
    dtype: int
    default: '-1'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import capture_tools
    make: capture_tools.add_rampup_cc(${alpha}, ${constant_start}, ${constant_stop},
        ${constant_length})

file_format: 1
