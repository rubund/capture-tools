# auto-generated by grc.converter

id: capture_tools_freqest
label: Find freq peak in FFT result
category: '[capture_tools]'

parameters:
-   id: sample_rate
    label: Sample rate
    dtype: float
-   id: data_rate
    label: Data rate
    dtype: float
-   id: fftlen
    label: FFT length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ fftlen }

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import capture_tools
    make: capture_tools.freqest(${sample_rate}, ${data_rate}, ${fftlen})

file_format: 1
