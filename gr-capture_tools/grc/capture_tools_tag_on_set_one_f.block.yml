# auto-generated by grc.converter

id: capture_tools_tag_on_set_one_f
label: tag_on_set_one_f
category: '[capture_tools]'

parameters:
-   id: tag_str
    label: tag
    dtype: string
    default: start
-   id: val
    label: Connect to button for instance
    dtype: int

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import capture_tools
    make: capture_tools.tag_on_set_one_f(${tag_str})
    callbacks:
    - self.${id}.set_value(${val})

file_format: 1
