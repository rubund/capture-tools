# auto-generated by grc.converter

id: capture_tools_add_at_tag_cc
label: add_at_tag_cc
category: '[capture_tools]'

parameters:
-   id: n_min
    label: N min
    dtype: int
-   id: n_max
    label: N max
    dtype: int
-   id: val
    label: Value
    dtype: complex
-   id: tag_key
    label: Tag key
    dtype: string

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import capture_tools
    make: capture_tools.add_at_tag_cc(${n_min}, ${n_max}, ${val}, ${tag_key})

file_format: 1
