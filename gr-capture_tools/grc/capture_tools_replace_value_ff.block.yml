# auto-generated by grc.converter

id: capture_tools_replace_value_ff
label: replace_value_ff
category: '[capture_tools]'

parameters:
-   id: search
    label: Search
    dtype: float
-   id: replace
    label: Replace
    dtype: float

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import capture_tools
    make: capture_tools.replace_value_ff(${search}, ${replace})

file_format: 1
