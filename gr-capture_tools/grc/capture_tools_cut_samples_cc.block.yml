# auto-generated by grc.converter

id: capture_tools_cut_samples_cc
label: cut_samples_cc
category: '[capture_tools]'

parameters:
-   id: n
    label: N
    dtype: int

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import capture_tools
    make: capture_tools.cut_samples_cc(${n})

file_format: 1
