# auto-generated by grc.converter

id: capture_tools_add_before_and_after_cc
label: add_before_and_after_cc
category: '[capture_tools]'

parameters:
-   id: n_before
    label: N samples before
    dtype: int
-   id: val_before
    label: Value before
    dtype: int
-   id: n_after
    label: N samples after
    dtype: int
-   id: val_after
    label: Value after
    dtype: int

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import capture_tools
    make: capture_tools.add_before_and_after_cc(${n_before}, ${n_after}, ${val_before},
        ${val_after})

file_format: 1
