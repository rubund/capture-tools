# auto-generated by grc.converter

id: capture_tools_save_iq_for_failed
label: save_iq_for_failed
category: '[capture_tools]'

parameters:
-   id: length_to_save
    label: Length to save
    dtype: int
    default: '1000'
-   id: history
    label: History
    dtype: int
    default: '1'
-   id: search_tag
    label: Search tag
    dtype: string
    default: '"address"'
-   id: max_in_queue
    label: Max in queue
    dtype: int
    default: '20'
-   id: save_path
    label: Save path
    dtype: string
-   id: mag_threshold
    label: mag_threshold
    dtype: float
    default: -1e12

inputs:
-   domain: stream
    dtype: complex
-   label: in_float_with_start_tag
    domain: stream
    dtype: float
    optional: true
-   domain: message
    id: passfail

templates:
    imports: import capture_tools
    make: |-
        capture_tools.save_iq_for_failed(${length_to_save}, ${max_in_queue}, ${save_path}, ${history})
        self.${id}.set_search_tag(${search_tag})
        self.${id}.set_mag_threshold(${mag_threshold})
    callbacks:
    - self.${id}.set_mag_threshold(${mag_threshold})
    - self.${id}.set_search_tag(${search_tag})

file_format: 1
