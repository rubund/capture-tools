# auto-generated by grc.converter

id: capture_tools_threshold_tag_other_stream_cc
label: threshold_tag_other_stream_cc
category: '[capture_tools]'

parameters:
-   id: vlen
    label: vlen
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: low_thresh
    label: low_thresh
    dtype: float
    default: '-0.1'
-   id: high_thresh
    label: high_thresh
    dtype: float
    default: '0.1'
-   id: going_high_tag
    label: Going high tag
    dtype: string
    default: '"going_high"'
-   id: going_low_tag
    label: Going low tag
    dtype: string
    default: '"going_low"'

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }
-   label: in_float
    domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

templates:
    imports: from gnuradio import capture_tools
    make: |-
        capture_tools.threshold_tag_other_stream_cc(${low_thresh}, ${high_thresh}, ${vlen})
        self.${id}.set_going_high_tag(${going_high_tag})
        self.${id}.set_going_low_tag(${going_low_tag})
    callbacks:
    - self.${id}.set_low_thresh(${low_thresh})
    - self.${id}.set_high_thresh(${high_thresh})
    - self.${id}.set_going_high_tag(${going_high_tag})
    - self.${id}.set_going_low_tag(${going_low_tag})

file_format: 1
