# auto-generated by grc.converter

id: capture_tools_annotated_to_msg_f
label: annotated_to_msg_f
category: '[capture_tools]'

parameters:
-   id: syncword
    label: Sync word (list)
    dtype: int_vector
-   id: packet_length
    label: Packet length
    dtype: int
    default: '100'

inputs:
-   domain: stream
    dtype: float
-   label: en
    domain: stream
    dtype: float

outputs:
-   domain: message
    id: packets

templates:
    imports: from gnuradio import capture_tools
    make: |-
        capture_tools.annotated_to_msg_f()
        self.${id}.set_sync_word(${syncword})
        self.${id}.set_packet_length(${packet_length})
    callbacks:
    - self.${id}.set_sync_word(${syncword})
    - self.${id}.set_packet_length(${packet_length})

file_format: 1
