# auto-generated by grc.converter

id: capture_tools_xrun_monitor_ff
label: xrun_monitor
category: capture_tools

parameters:
-   id: length
    label: length
    dtype: int
    default: '500000'
-   id: drop_when_full
    label: Drop when full
    dtype: bool
    default: 'False'
-   id: type
    label: IO Type
    dtype: enum
    options: [float, complex]
    option_attributes:
        const_type: [real_vector, complex_vector]
        fcn: [ff, cc]
    hide: part

inputs:
-   domain: stream
    dtype: ${ type }

outputs:
-   domain: stream
    dtype: ${ type }

templates:
    imports: import capture_tools
    make: |-
        capture_tools.xrun_monitor_${type.fcn}(${length})
        self.${id}.set_drop_when_full(${drop_when_full})

file_format: 1
